import 'package:flutter/material.dart';

class DummyTaskInfo extends StatelessWidget {
  const DummyTaskInfo({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}